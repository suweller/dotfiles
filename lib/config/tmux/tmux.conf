# vim:fdm=marker
set -g automatic-rename on
set -g default-shell $SHELL
set -ga terminal-overrides ",xterm*:Tc"
set -g focus-events on
set -g escape-time 0 # decrease command delay (increases vim responsiveness) # NOTE: removed -s
set -g history-limit 100000
set -g mode-keys vi
set -g monitor-activity on
set -g mouse
set -g renumber-windows on
set -g set-titles off
set -g status on
set -g status-position top
set -g status-bg colour234
set -g status-fg colour238

set-window -g pane-base-index 0
set-window -g xterm-keys on # to make ctrl-arrow, etc. work

bind -r H resize-pane -L 10
bind -r J resize-pane -D 10
bind -r K resize-pane -U 10
bind -r L resize-pane -R 10
bind -r s run-shell "tmux setw -g status \$(tmux show -g -w status | grep -q off && echo on || echo off)"

# Open new window with current path
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
bind -r Left swap-window -t -1
bind -r Right swap-window -t +1

bind -r g run -b '~/.dotfiles/bin/tmux-open-app || exit 0'
# bind X if-shell -F '#{?pane_input_off,1,0}'
#     "select-pane -e \; select-pane -T '#{@original_pane_title}'" \
#     "run-shell \"tmux set-option -p @original_pane_title \\\"\\\$(tmux display-message -p -F '#{pane_title}')\\\"\" \; select-pane -d \; select-pane -T '#{pane_title} [LOCKED]'"

# fix ssh agent when tmux is detached
setenv -g SSH_AUTH_SOCK $HOME/.ssh/ssh_auth_sock

set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'
set -g @plugin 'sainnhe/tmux-fzf' # {{{ Prefix F -
# }}}

set -g @plugin 'tmux-plugins/tmux-yank' # {{{
set -g @tmux-yank-start-selection 'v'
# }}}

set -g @plugin 'wfxr/tmux-fzf-url' # {{{
set -g @fzf-url-extra-filter 'grep -oE "zgenom load \b[\w\/]\b"'
# }}}

set -g @plugin 'roosta/tmux-fuzzback' # {{{ Prefix ? - fzf history
# }}}

set -g status-left ' '
set -g status-bg black
set -g status-fg white
# set -g window-status-format "#[bg=black,fg=color19]#[bg=color19,fg=black]#I #W#[bg=black,fg=color19]"
# set -g window-status-current-format "#[bg=black,fg=color16]#[bg=color16,fg=black]#I #W#[bg=black,fg=color16]"
# set -g window-status-format "#[bg=color19,fg=black] #I #W "
# set -g window-status-current-format "#[bg=color16,fg=black] #I #W "
# set -g window-status-activity-style reverse

# set -g status-right "#[fg=color08,bg=black]#[fg=color09,bg=color08]#{pomodoro_status} "
# set -ga status-right "#[fg=color20,bg=color08]#[fg=color08,bg=color20]%m%d󱛡%H%M "
# set -ga status-right "#[fg=color21,bg=color20]#[fg=color08,bg=color21]#(/Users/suweller/.dotfiles/bin/battery.sh -di) "
set -g status-right "#[fg=color08,bg=black]#[fg=color09,bg=color08]"
set -ga status-right " #{pomodoro_status} "
set -ga status-right "#[fg=color20,bg=color08]#[fg=color08,bg=color20]"
set -ga status-right " #(/Users/suweller/.dotfiles/datetime) "
set -ga status-right "#[fg=color21,bg=color20]#[fg=color08,bg=color21]"
set -ga status-right " #(/Users/suweller/.dotfiles/bin/battery -sb) "
set -g @plugin 'tinted-theming/tinted-tmux' # {{{
run-shell "tmux set-option -g @tinted-color $(tinty current)"
# }}}

set -g @plugin 'olimorris/tmux-pomodoro-plus' # {{{
set -g @pomodoro_cancel 'T'
set -g @pomodoro_complete "#[fg=color20]󰗇"
set -g @pomodoro_granularity 'off'
set -g @pomodoro_interval_display 'off'
set -g @pomodoro_notifications 'on'
set -g @pomodoro_on "#[fg=color10]󰐑"
set -g @pomodoro_pause "#[fg=color11]󰐓"
set -g @pomodoro_prompt_break "#[fg=color12]󰷲?"
set -g @pomodoro_prompt_pomodoro "#[fg=color09]󰷲?"
set -g @pomodoro_sound '/System/Library/Sounds/Purr.aiff'
set -g @pomodoro_toggle 't'
# }}}

set -g @plugin 'tmux-plugins/tpm' # {{{
# Prefix I - Install plugins & Refresh TMUX env
# Prefix U - Update plugins & Refresh TMUX env
# Prefix alt u - Remove/Uninstall plugins not in the list above
# }}}

if "test ! -d $XDG_CONFIG_HOME/tmux/plugins/tpm" \
  "run 'git clone https://github.com/tmux-plugins/tpm $XDG_CONFIG_HOME/tmux/plugins/tpm'"
run "$XDG_CONFIG_HOME/tmux/plugins/tpm/tpm"
